{
  "name": "claude-setup",
  "version": "1.0.0",
  "private": true,
  "description": "Template base per progetti universitari con Claude Code",
  "scripts": {
    "build": "echo \"No build configured yet\"",
    "test": "echo \"No tests configured yet\"",
    "lint": "echo \"No linter configured yet\""
  }
}
